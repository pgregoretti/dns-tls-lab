services:
  nginx:
    image: nginx:latest
    container_name: dns-tls-lab
    ports:
      - "8080:80"
      - "8443:443"
    volumes:
      - ./nginx/default.conf:/etc/nginx/conf.d/default.conf:ro
      - ./nginx/certs:/etc/nginx/certs:ro
      - ./app:/usr/share/nginx/html:ro